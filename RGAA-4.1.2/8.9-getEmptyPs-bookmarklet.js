javascript:(function getEmptyPs() { const paragraphs = document.querySelectorAll('p'); const emptyPs = []; paragraphs.forEach(p => { const content = p.innerHTML.trim(); if (content === '&nbsp;' || content == '') { emptyPs.push(p); } }); const countEmptyPs = emptyPs.length; if (countEmptyPs === 0) { alert("Pas de paragraphes vides"); return; } let message = countEmptyPs + " paragraphes vides"; if (countEmptyPs === 1) { message = message.replace('vides', 'vide'); } alert(message); emptyPs.forEach(p => console.log(p)); })();